# MikeCVermeer Homebrew Tap

Homebrew casks and formulae for MikeCVermeer's projects.

## NoSleep

Install NoSleep:

```bash
brew install --cask MikeCVermeer/tap/nosleep
```

Upgrade later:

```bash
brew update
brew upgrade --cask nosleep
```

Uninstall:

```bash
brew uninstall --cask nosleep
```
